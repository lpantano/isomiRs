language: r
dist: trusty
cache: packages
r: bioc-devel
r_github_packages:
  - jimhester/covr
before_install:
  - sudo apt-get install texlive-fonts-extra
after_success:
  - Rscript -e 'covr::codecov()'
