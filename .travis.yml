language: r
sudo: false
dist: trusty
cache:
  packages: true
r: bioc-devel
r_github_packages:
  - jimhester/covr
before_install:
  - tlmgr install bera
  - tlmgr install nowidow
  - tlmgr install parnotes
after_success:
  - Rscript -e 'covr::codecov()'
